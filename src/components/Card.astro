<div class="card">
    <div class="card-content-border">
        <div class="card-content" style="background-image: url(<slot name='background-image'></slot>)">
            <div class="card-inner-content">
                <slot />
            </div>
        </div>
    </div>
</div>

<style>
    :root {
        --border-thickness: 2px;
        --border-radius: 5px;
        --cut-off-size: 40px;
        --padding-size: 20px;
    }

    .card {
        max-width: 600px;
        margin: 0 auto;
        position: relative;
        padding: var(--border-thickness);
        width: 100%;
        box-sizing: border-box;
    }

    .card-content-border {
        position: relative;
        width: 100%;
        height: 100%;
        padding: var(--border-thickness);
        background: linear-gradient(135deg, rgba(76, 186, 243, 0.8) 0%, rgba(253, 38, 83, 0.8) 100%);
        border-radius: var(--border-radius);
        clip-path: polygon(0 0, 100% 0, 100% calc(100% - var(--cut-off-size)), calc(100% - var(--cut-off-size)) 100%, 0 100%);
    }

    .card-content {
        position: relative;
        width: 100%;
        height: 100%;
        background-color: #030108;
        border-radius: var(--border-radius);
        padding: 0;
        clip-path: polygon(0 0, 100% 0, 100% calc(100% - var(--cut-off-size)), calc(100% - var(--cut-off-size)) 100%, 0 100%);
        box-sizing: border-box;
        background-size: cover;
        background-position: center;
    }

    .card-inner-content {
        padding: var(--padding-size);
    }
</style>